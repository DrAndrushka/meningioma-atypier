# ═══════════════════════════════════════════════════════════════════════════════
# 🧠 meningioma-atypier — Python dependencies
# ═══════════════════════════════════════════════════════════════════════════════
#
# Python 3.11+ recommended (devcontainer uses 3.11; tested on 3.12).
#
# Install:
#   pip install -r requirements.txt
#
# Run tests:
#   cd meningioma-atypier
#   python -m pytest
#
# Launch calculator:
#   streamlit run app.py
# ═══════════════════════════════════════════════════════════════════════════════


# ───────────────────────────────────────────────────────────────────────────────
# 📊 Core analysis pipeline
#    cleaning → schema → DDA → EDA → diagnostic accuracy → MICE →
#    multivariable logistic (multi-variant) → bootstrap validation → HTML report
# ───────────────────────────────────────────────────────────────────────────────

pandas>=2.2,<4
# Tabular data: cohort loading, schema application, CSV/Excel I/O, pipeline tables.

numpy>=2.0,<3
# Numeric arrays underpinning stats, design matrices, and imputation.

scipy>=1.11
# Classical tests (Mann–Whitney, χ², Fisher, skew/kurtosis) and calibration root-finding.

statsmodels>=0.14
# Multivariable logistic regression (Logit), VIF, Wilson-style proportion CIs, calibration.

scikit-learn>=1.4
# MICE (IterativeImputer + RandomForestRegressor), bootstrap metrics (AUC, Brier, ROC).

matplotlib>=3.8
# Distribution plots (DDA), forest plots, validation charts, missingness bars.

seaborn>=0.13
# EDA association plots and missingness heatmaps.

openpyxl>=3.1
# Read Excel cohort files when the raw export is .xlsx (CSV also supported).


# ───────────────────────────────────────────────────────────────────────────────
# 🌐 Streamlit clinical calculator (app.py)
#    Reads portable JSON artifacts from model_artifacts/ — no notebook required at runtime.
# ───────────────────────────────────────────────────────────────────────────────

streamlit>=1.28
# Interactive high-grade meningioma risk calculator UI.


# ───────────────────────────────────────────────────────────────────────────────
# 🧪 Tests (pytests_atypier/)
#    140+ tests covering cleaning, EDA, diagnostic accuracy, inferential pooling,
#    multi-variant artifacts, report, and calculator.
# ───────────────────────────────────────────────────────────────────────────────

pytest>=8.0
# Unit and integration tests for the heavy_machinery modules.


# ───────────────────────────────────────────────────────────────────────────────
# 📓 Notebook workflow (optional — not needed for Streamlit or pytest alone)
#    heavy_machinery/meningioma.ipynb orchestrates the full pipeline top-to-bottom.
#    Config steps use IPython.display for inline schema / filter previews.
# ───────────────────────────────────────────────────────────────────────────────

ipython>=8.0
# Inline display helpers in config steps (§04 row filters, §06 derivations).

jupyter>=1.0
# Run the main notebook: jupyter notebook heavy_machinery/meningioma.ipynb
