# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
dist/
build/

# Test / type / lint caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Secrets
.env
.streamlit/secrets.toml

# PHI & local inputs (*.xlsx anywhere, e.g. heavy_machinery/)
*.xlsx
*.xls
*.csv
data/

# Generated pipeline outputs (anywhere under the repo)
output/
outputs/
model_artifacts/
*.parquet
*.tmp
dupes.csv

# Notebooks
.ipynb_checkpoints/
aesthetics_experiments.ipynb

# R (formal MICE engine, heavy_machinery/scripts/run_mice.R). The engine itself is tracked;
# these are stray runtime artifacts. MICE exchange/run files live under output/
# (output/missingness/mice/r_run/) and are already ignored via output/.
.Rhistory
.RData
.Rapp.history
.Rproj.user/
Rplots.pdf

# OS / editor
.DS_Store
.vscode/
.idea/
Icon?

# Nested git clone — use only this repo
heavy_machinery/.git/

# subagent-driven-development scratch (ledger, briefs, review packages)
.superpowers/

# Manuscript drafts and design scratch under docs/. The plans and specs in
# docs/superpowers/ stay tracked; these are working copies that change every
# session — the live Statistics/Results markdown, its rewrite, the Word export
# of that rewrite, and the design canvas for Table 1.
docs/stats_results_live.md
docs/stats_results_live_rewrite.md
docs/*.docx
docs/design/
